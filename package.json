{
  "author": "Andreas Monitzer <andreas@monitzer.com>",
  "name": "webgl_rust_demo",
  "version": "0.1.0",
  "repository": "https://github.com/anlumo/webgl_rust_demo.git",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist pkg && webpack",
    "start": "rimraf dist pkg && webpack-dev-server --open -d",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^0.4.2",
    "copy-webpack-plugin": "^11.0.0",
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^3.7.1",
    "rimraf": "^2.6.3"
  }
}
